{
    "name": "my-blog",
    "version": "1.0.0",
    "private":  true,
    "scripts": {
        "serve-dev": "set NODE_ENV=development && npm run serve",
        "serve-production": "set NODE_ENV=production && npm run serve",
        "serve": "node node_modules/mimo-theme/dist/tools/serve.js -p ../../../../ -n ../../../",
        "build-dev": "set NODE_ENV=development && npm run build",
        "build-production": "set NODE_ENV=production && npm run build",
        "build": "node node_modules/mimo-theme/dist/tools/build.js -p ../../../../ -n ../../../"
    },
    "dependencies": {},
    "devDependencies": {
        "mimo-theme": "latest"
    }
}
