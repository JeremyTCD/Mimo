{
    "name": "my-blog",
    "version": "1.0.0",
    "main": "index.js",
    "license": "MIT",
    "scripts": {
        "serve-dev": "set NODE_ENV=development && yarn run serve",
        "serve-production": "set NODE_ENV=production && yarn run serve",
        "serve": "node node_modules/jeremytcd-docfx-themes-mimo/dist/tools/serve.js -p ../../../../ -n ../../../",
        "build-dev": "set NODE_ENV=development && yarn run build",
        "build-production": "set NODE_ENV=production && yarn run build",
        "build": "node tools/build.js -p ../../../../ -n ../../../"
    },
    "dependencies": {},
    "devDependencies": {
        "jeremytcd-docfx-themes-mimo": "latest"
    }
}
