{{!include(/^styles/.*/)}}
{{!include(/^fonts/.*/)}}
<!DOCTYPE html>

<html {{#mimo_pageID}}id={{mimo_pageID}}{{/mimo_pageID}}>
    {{>partials/head}}
    <body>
        {{>partials/pageHeader}}

        {{#mimo_outerCoreNumber}}
        <div id="core-{{{mimo_outerCoreNumber}}}">
        {{/mimo_outerCoreNumber}}

            {{>partials/sectionMenu}}

            {{#mimo_innerCoreNumber}}
            <div id="core-{{{mimo_innerCoreNumber}}}">
            {{/mimo_innerCoreNumber}}

                {{>partials/main}}
		        {{>partials/articleMenu}}

            {{#mimo_innerCoreNumber}}
            </div>
            {{/mimo_innerCoreNumber}}          

        {{#mimo_outerCoreNumber}}
        </div>
        {{/mimo_outerCoreNumber}}

        {{>partials/footer}}
        <div id="overlay"></div>
    </body>
</html>
